# 📖 دليل التطوير - Battery Sales Manager

دليل شامل لمطوري المشروع يتضمن أفضل الممارسات والمعايير والإرشادات الحالية.

---

## 🏗️ معمارية التطبيق (Architecture)

يتبع المشروع معمارية **MVVM (Model-View-ViewModel)** مع استخدام **Hilt** لحقن التبعيات و **Coroutines** للعمليات غير المتزامنة.

### الطبقات الرئيسية:
1. **UI Layer**: بناء الواجهات باستخدام **Jetpack Compose**.
2. **ViewModel Layer**: إدارة حالة الواجهة ومعالجة المنطق البرمجي.
3. **Repository Layer**: وسيط بين التطبيق ومصادر البيانات (Firestore).
4. **Data Models**: تعريف هياكل البيانات باستخدام Kotlin Data Classes.

---

## 🎨 واجهة المستخدم والمعايير (UI Standards)

للحفاظ على تناسق التطبيق، يجب اتباع القواعد التالية:

### 1. **الترويسة الموحدة (SharedHeader)**
يجب استخدام المكون `SharedHeader` في جميع الشاشات. يتميز بتصميم تكيفي يدعم أحجام الخطوط الكبيرة:
```kotlin
SharedHeader(
    title = "عنوان الشاشة",
    onBackClick = { navController.popBackStack() },
    actions = {
        HeaderIconButton(icon = Icons.Default.Settings, onClick = { /* Action when Settings is clicked */ })
        HeaderIconButton(icon = Icons.Default.Logout, onClick = { /* Action when Logout is clicked */ })
    }
)
```

### 2. **الأزرار القياسية**
تم توحيد نصوص أزرار التأكيد والإلغاء في جميع النوافذ المنبثقة (Dialogs):
- أزرار التأكيد: **"موافق"**
- أزرار الإلغاء: **"إلغاء"**

### 3. **التكيف مع الخط (Accessibility)**
يجب تجنب استخدام أحجام `sp` الثابتة للنصوص واستبدالها بستايلات `MaterialTheme.typography` لضمان تكيف الواجهة مع إعدادات تكبير الخط في النظام.

---

## 🔔 الخدمات والخلفية (Services)

### **NotificationService**
خدمة تعمل في الخلفية (`START_STICKY`) تضمن استمرار عمل مستمعات Firebase (Listeners) حتى عند إغلاق التطبيق.
- وظيفتها: مراقبة طلبات الموافقة، انخفاض المخزون، والكمبيالات المستحقة.
- يجب التأكد من تسجيلها في `AndroidManifest.xml`.

---

## 📊 التعامل مع البيانات

### 1. **دقة الأرقام (Precision)**
في العمليات الحسابية الخاصة بالبطاريات القديمة (السكراب) والمحاسبة، يجب استخدام **3 خانات عشرية (0.000)** لضمان الدقة المطلوبة.

### 2. **الفرز الزمني**
يجب عرض البيانات (فواتير، عمليات مالية، مخزون) بترتيب **تنازلي** حسب تاريخ التعديل `updatedAt` أو `timestamp` لضمان ظهور أحدث العمليات في الأعلى.

---

## 📋 قائمة التحقق (Checklist) للميزات الجديدة

- [ ] استخدام `SharedHeader` للشاشة الجديدة.
- [ ] دعم الوضع الليلي (Dark Mode) باستخدام الألوان من `MaterialTheme.colorScheme`.
- [ ] التأكد من أن جميع الحقول النصية تستخدم `CustomKeyboardTextField` لدعم الكيبورد المدمج.
- [ ] استخدام `AppDateRangePickerDialog` لعمليات فلترة التاريخ لضمان التناسق.
- [ ] التحقق من صلاحيات المستخدم (Admin vs Seller) لإخفاء/إظهار العناصر الحساسة.

---

## 🤝 خطوات التطوير

1. أنشئ فرع جديد: `git checkout -b feature/feature-name`
2. اتبع معايير الترميز وسمِّ المتغيرات بوضوح (مثلاً: `isSeller`, `warehouseId`).
3. اختبر الواجهة على أحجام خطوط مختلفة (1.0x و 2.0x).
4. تأكد من نجاح عملية البناء: `./gradlew compileDebugKotlin`.
5. أرسل Pull Request للمراجعة.

---

**آخر تحديث**: 2024-05-24
